# 6.19 鎸夐敭娑堟姈妯″潡鍖栧皝瑁?
> **鑺墖**锛歋TM32F103C8T6 | **鐜**锛欳ubeMX + VSCode + Keil  
> **涓婚**锛氭寜閿秷鎶栫姸鎬佹満鐙珛灏佽锛坘ey_debounce_t锛夈€佸洖璋冨嚱鏁版敞鍐屻€乷sKernelGetTickCount

> 馃敆 琛旀帴 6.18锛?.18 涓寜閿秷鎶栭€昏緫鍐呭祵鍦?`vIP_KEYTask` 閲岋紱6.19 灏嗗叾鎶借薄涓虹嫭绔嬫ā鍧?`My_Code2.h/.c`锛岄€氳繃鍥炶皟鍑芥暟涓庝笟鍔¤В鑰︼紝鍙洿鎺ユ浛鎹?6.18 涓殑娑堟姈浠ｇ爜銆?
---

## 涓€銆佸畬鏁翠唬鐮?
### My_Code2.h

```c
#ifndef INC_MY_CODE2_H_
#define INC_MY_CODE2_H_

#include "My_Code1.h"

#define KEY_DEBOUCNE_TIME_MS 20

// 鎸夐敭娑堟姈鍥炶皟
typedef void(*key_callback_t)(void);

// 鎸夐敭娑堟姈鐘舵€佹満
typedef enum {
    key_idle,              // 鎸夐敭绌洪棽
    key_press_debounce,    // 鎸変笅娑堟姈
    key_pressed,           // 纭鎸変笅
    key_release_debounce   // 閲婃斁娑堟姈
} KEY_DeBounce_StateMachine;

// 鎸夐敭娑堟姈鐘舵€佹満灏佽
typedef struct {
    GPIO_TypeDef *port;
    uint16_t pin;
    KEY_DeBounce_StateMachine key_debounce_state;
    uint32_t key_debounce_tick;     // 鎸夐敭娑堟姈涓撶敤鏃堕棿
    key_callback_t on_press;        // 鎸変笅鍥炶皟
} key_debounce_t;

// 鍏ㄥ眬鎸夐敭瀹炰緥
extern key_debounce_t key1; // 鎸夐敭1
extern key_debounce_t key2; // 鎸夐敭2

// 鎸夐敭娑堟姈鐘舵€佹満鍑芥暟
void KEY_DeBounce_State_Machine(key_debounce_t *key);

// 鎸夐敭娑堟姈纭鎸変笅鍥炶皟鍑芥暟
void KEY_SetPressCallback(key_debounce_t *key, key_callback_t callback);

// 鍥炶皟鍑芥暟锛氭寜涓嬫寜閽彂鐢熺殑鎿嶄綔
void KEY1_Callback(void);
void KEY2_Callback(void);

// 鍒濆鍖栨寜閿秷鎶?void KEY_DeBounce_Init(void);

#endif /* INC_MY_CODE2_H_ */
```

### My_Code2.c

```c
#include "My_Code2.h"

// 鍒濆鍖栨寜閿?瀹炰緥
key_debounce_t key1 = {
    .port = GPIOA,
    .pin = GPIO_PIN_1,
    .key_debounce_state = key_idle,
    .key_debounce_tick = 0,
    .on_press = NULL
};

// 鍒濆鍖栨寜閿?瀹炰緥
key_debounce_t key2 = {
    .port = GPIOA,
    .pin = GPIO_PIN_2,
    .key_debounce_state = key_idle,
    .key_debounce_tick = 0,
    .on_press = NULL
};

void KEY_DeBounce_State_Machine(key_debounce_t *key)
{
    uint8_t key_level = HAL_GPIO_ReadPin(key->port, key->pin);

    switch (key->key_debounce_state)
    {
    case key_idle:
        if (key_level == GPIO_PIN_RESET) {
            key->key_debounce_tick = osKernelGetTickCount();  // 1 tick = 1ms
            key->key_debounce_state = key_press_debounce;
        }
        break;

    case key_press_debounce:
        if (key_level != GPIO_PIN_RESET) {
            key->key_debounce_state = key_idle;
        } else {
            if (osKernelGetTickCount() - key->key_debounce_tick > KEY_DEBOUCNE_TIME_MS) {
                key->key_debounce_state = key_pressed;
                if (key->on_press) key->on_press();  // 纭鎸変笅 鈫?鍥炶皟
            }
        }
        break;

    case key_pressed:
        if (key_level != GPIO_PIN_RESET) {
            key->key_debounce_tick = osKernelGetTickCount();
            key->key_debounce_state = key_release_debounce;
        }
        break;

    case key_release_debounce:
        if (key_level == GPIO_PIN_RESET) {
            key->key_debounce_state = key_pressed;
        } else if (key_level != GPIO_PIN_RESET) {
            if (osKernelGetTickCount() - key->key_debounce_tick > KEY_DEBOUCNE_TIME_MS) {
                key->key_debounce_state = key_idle;
            }
        }
        break;
    }
}

void KEY_SetPressCallback(key_debounce_t *key, key_callback_t callback)
{
    key->on_press = callback;
}

// 浣跨敤鎸夐敭鍥炶皟鏃讹紝鏀惧埌鏈€涓婇潰鍒濆鍖?void KEY_DeBounce_Init(void)
{
    KEY_SetPressCallback(&key1, KEY1_Callback);
    KEY_SetPressCallback(&key2, KEY2_Callback);
}

// 鍥炶皟鍑芥暟锛氭寜涓嬫寜閽彂鐢熺殑鎿嶄綔
void KEY1_Callback(void)
{
    // 绀轰緥锛氬浼犻€?keymessage
    // KEYMessage msg = {.w25q64_state = W25Q64_Save_State};
    // osMessageQueuePut(KEY_QueueHandle, &msg, 0, 0);
}

void KEY2_Callback(void)
{
    // 绀轰緥锛氬浼犻€?keymessage
    // KEYMessage msg = {.w25q64_state = W25Q64_State_Recovery};
    // osMessageQueuePut(KEY_QueueHandle, &msg, 0, 0);
}
```

---

## 浜屻€佽鐐?
### 1. 涓?6.18 鐨勮仈绯?
| 6.18锛堝唴宓岀増锛?| 6.19锛堟ā鍧楀寲鐗堬級 |
|----------------|-----------------|
| 娑堟姈閫昏緫鍐欏湪 `vIP_KEYTask` 閲?| 鎶借薄涓?`key_debounce_t` 缁撴瀯浣?|
| 鎸夐敭瀹炰緥闈犲眬閮ㄥ彉閲?| 鍏ㄥ眬缁撴瀯浣?+ 鎸囧畾鍒濆鍖栧櫒 |
| 鐢?`HAL_GPIO_ReadPin` 鐩磋 | 鐘舵€佹満灏佽锛屼富寰幆璋?`KEY_DeBounce_State_Machine()` |
| 鍥炶皟鏈嫭绔?| `on_press` 鍑芥暟鎸囬拡娉ㄥ唽鍥炶皟 |

### 2. 鏂板彉鍖?
- **`osKernelGetTickCount()`**锛氭浛浠ｈ８鏈虹殑 `GetTick()`锛孎reeRTOS 鍘熺敓 tick 璁℃暟
- **鎸囧畾鍒濆鍖栧櫒**锛歚{ .port = GPIOA, .pin = GPIO_PIN_1, ... }` 鏇存竻鏅?- **鍥炶皟妯℃澘鐣欑┖**锛歚KEY1_Callback` / `KEY2_Callback` 鐣欏緟涓氬姟濉厖锛堝 6.18 鐨勯槦鍒楀彂閫侊級

### 3. 浣跨敤鏂瑰紡

```c
// 鍒濆鍖?KEY_DeBounce_Init();

// 涓诲惊鐜垨浠诲姟涓畾鏃惰皟鐢?KEY_DeBounce_State_Machine(&key1);
KEY_DeBounce_State_Machine(&key2);
```
