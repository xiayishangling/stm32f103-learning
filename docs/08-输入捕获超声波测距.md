# 6.9 杈撳叆鎹曡幏 + 瓒呭０娉㈡祴璺濓紙鐘舵€佹満閲嶆瀯锛?
> **鑺墖**锛歋TM32F103C8T6 | **鐜**锛歏SCode + Keil + Keil Assistant  
> **涓婚**锛氳緭鍏ユ崟鑾枫€佸弻閫氶亾鍗忓悓銆佸洖缁曡绠椼€佺姸鎬佹満閲嶆瀯銆佸井绉掔骇璁℃椂鍣?
---

## 涓€銆佹牳蹇冧唬鐮?
### 1. 寰绾ц鏃跺櫒锛圱IM3 + 婧㈠嚭璁℃暟锛?
```c
volatile uint32_t timer3_overflow_count = 0;

void us_TIM(void)
{
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM3, ENABLE);
    TIM_TimeBaseInitTypeDef tim = {0};
    tim.TIM_Prescaler = 71;        // 1MHz = 1渭s
    tim.TIM_Period    = 0xFFFF;    // 65ms 婧㈠嚭涓€娆?    tim.TIM_CounterMode = TIM_CounterMode_Up;
    TIM_TimeBaseInit(TIM3, &tim);
    TIM_ITConfig(TIM3, TIM_IT_Update, ENABLE);
    TIM_Cmd(TIM3, ENABLE);
}

void TIM3_IRQHandler(void)
{
    if (TIM_GetFlagStatus(TIM3, TIM_FLAG_Update) == SET) {
        TIM_ClearFlag(TIM3, TIM_FLAG_Update);
        timer3_overflow_count++;    // 姣?65ms 璁颁竴娆℃孩鍑?    }
}

// 鑾峰彇褰撳墠鎬诲井绉掓暟锛堥槻婧㈠嚭绔炴€侊級
uint32_t micros(void)
{
    uint32_t ovf, cnt;
    do {
        ovf = timer3_overflow_count;
        cnt = TIM3->CNT;
    } while (ovf != timer3_overflow_count);  // 璇诲埌涓€鍗婃孩鍑轰簡锛熼噸鏉?    return (ovf << 16) | cnt;  // 楂?6浣?婧㈠嚭娆℃暟锛屼綆16浣?CNT
}

void My_Delay_2_us(uint32_t us)
{
    uint32_t start = micros();
    while ((micros() - start) < us);
}
```

### 2. 杈撳叆鎹曡幏鍒濆鍖栵紙鍙岄€氶亾鍗忓悓锛?
```c
void Init_Timebase_InputCapture(void)
{
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_TIM1, ENABLE);

    // 鏃跺熀锛?渭s 鍒嗚鲸鐜囷紝65ms 鍛ㄦ湡
    TIM_TimeBaseInitTypeDef tim = {0};
    tim.TIM_Prescaler = 71;
    tim.TIM_Period    = 65535;
    TIM_TimeBaseInit(TIM1, &tim);

    // CH1锛氱洿鎺ヤ笂鍗囨部鎹曡幏
    TIM_ICInitTypeDef ic = {0};
    ic.TIM_Channel    = TIM_Channel_1;
    ic.TIM_ICPolarity = TIM_ICPolarity_Rising;
    ic.TIM_ICSelection = TIM_ICSelection_DirectTI;   // 鐩存帴
    ic.TIM_ICFilter   = 0x0;
    ic.TIM_ICPrescaler = TIM_ICPSC_DIV1;
    TIM_ICInit(TIM1, &ic);

    // CH2锛氶棿鎺ヤ笅闄嶆部鎹曡幏锛堝悓寮曡剼 PA8锛屾崟鑾烽珮鐢靛钩缁撴潫鏃跺埢锛?    ic.TIM_Channel    = TIM_Channel_2;
    ic.TIM_ICPolarity = TIM_ICPolarity_Falling;
    ic.TIM_ICSelection = TIM_ICSelection_IndirectTI; // 闂存帴
    TIM_ICInit(TIM1, &ic);

    TIM_ITConfig(TIM1, TIM_IT_CC1 | TIM_IT_CC2, ENABLE);
    TIM_Cmd(TIM1, ENABLE);
}
```

### 3. 鎹曡幏涓柇锛堝彧璁炬爣蹇椾綅锛?
```c
void TIM1_CC_IRQHandler(void)
{
    if (TIM_GetFlagStatus(TIM1, TIM_FLAG_CC1) == SET) {
        TIM_ClearFlag(TIM1, TIM_FLAG_CC1);
        capture_state |= 0x01;   // 涓婂崌娌挎崟鑾峰埌浣?    }
    if (TIM_GetFlagStatus(TIM1, TIM_FLAG_CC2) == SET) {
        TIM_ClearFlag(TIM1, TIM_FLAG_CC2);
        capture_state |= 0x02;   // 涓嬮檷娌挎崟鑾峰埌浣?    }
    if (capture_state == 0x03) { // 鍙屾部閮藉埌浜?        Rang_State = RANG_OK;
        capture_state = 0;
    }
}
```

### 4. 瓒呭０娉㈡祴璺濈姸鎬佹満

```c
typedef enum { TRIG_SEND, WAIT_ECHO, PROCESS, INTERVAL } state_t;
state_t state = TRIG_SEND;

void ultrasonic_ranging_state_machine(void)
{
    switch (state)
    {
    case TRIG_SEND:
        TIM_SetCounter(TIM1, 0);
        TIM_ClearFlag(TIM1, TIM_FLAG_CC1 | TIM_FLAG_CC2);
        Rang_State = RANG_NO;
        capture_state = 0;

        GPIO_WriteBit(GPIOA, GPIO_Pin_1, Bit_SET);  // Trig 鎷夐珮
        My_Delay_2_us(10);                           // 10渭s
        GPIO_WriteBit(GPIOA, GPIO_Pin_1, Bit_RESET);

        start_time = currentTick;
        state = WAIT_ECHO;
        break;

    case WAIT_ECHO:
        if (Rang_State == RANG_OK)      { state = PROCESS; break; }
        if (currentTick - start_time > 30) { state = INTERVAL; }  // 30ms 瓒呮椂
        break;

    case PROCESS:
        ccr1 = TIM_GetCapture1(TIM1);
        ccr2 = TIM_GetCapture2(TIM1);
        // 鍥炵粫瀹夊叏璁＄畻
        uint32_t width = (ccr2 >= ccr1)
            ? (ccr2 - ccr1)
            : (0xFFFF - ccr1 + ccr2 + 1);  // us

        if (width > 100 && width < 20000) {  // 1.7cm ~ 3.4m
            float dist = (width * 1.0e-6f * 340.0f) / 2.0f;
            printf("%.3f m\r\n", dist);
            LED_MODE = LED_ON;              // 娴嬪埌璺濈浜伅 0.5s
        }
        start_time = currentTick;
        state = INTERVAL;
        break;

    case INTERVAL:
        TIM_Cmd(TIM1, DISABLE);             // 绌洪棽鍏冲畾鏃跺櫒鐪佺數
        if (currentTick - start_time > 2000) // 2s 闂撮殧
            state = TRIG_SEND;
        break;
    }
}
```

---

## 浜屻€佹牳蹇冪煡璇嗙偣

### 1. 杈撳叆鎹曡幏淇″彿閫氳矾

```
ECHO 寮曡剼 鈫?杈撳叆婊ゆ尝(鍘绘瘺鍒? 鈫?杈规部妫€娴?鈫?淇″彿閫夋嫨(鐩存帴/闂存帴/TRC)
         鈫?鍒嗛鍣?鈫?閿佸瓨 CNT 鍒?CCRx 鈫?瑙﹀彂涓柇
```

### 2. 鍙岄€氶亾鍗忓悓鍘熺悊

| 閫氶亾 | 杈规部 | 閫夋嫨 | 浣滅敤 |
|------|------|------|------|
| CH1 | 涓婂崌娌?| 鐩存帴 | 閿佸瓨楂樼數骞冲紑濮嬫椂鍒?鈫?ccr1 |
| CH2 | 涓嬮檷娌?| 闂存帴 | 閿佸瓨楂樼數骞崇粨鏉熸椂鍒?鈫?ccr2 |

> 鍚屽紩鑴?PA8锛屼竴娆″畬鏁磋剦鍐插緱鍒颁袱涓椂闂存埑锛宍ccr2 - ccr1` = 楂樼數骞冲搴?
### 3. 鍥炵粫璁＄畻鐨?int 鎻愬崌闄烽槺

```c
// 鉂?鍗遍櫓锛歶int16_t 杩愮畻鍓嶉殣寮忔彁鍗囦负鏈夌鍙?int
uint32_t width = ccr2 - ccr1;  // ccr2 < ccr1 鏃跺緱璐熸暟锛岃浆 u32 鍙樺法澶у€?
// 鉁?娉曚竴锛氭樉寮忓垽鏂洖缁?uint32_t width = (ccr2 >= ccr1) ? (ccr2 - ccr1) : (0xFFFF - ccr1 + ccr2 + 1);

// 鉁?娉曚簩锛?6 浣嶆ā杩愮畻鑷姩淇
uint32_t width = (uint16_t)(ccr2 - ccr1);
```

### 4. 瓒呭０娉㈡祴璺濆叕寮?
```
璺濈 = (楂樼數骞虫椂闂?脳 澹伴€? / 2
     = (width 渭s 脳 10鈦烩伓 脳 340 m/s) / 2
```

> `/2` 鏄洜涓哄０娉㈠線杩旓紝鍗曠▼璺濈闇€闄や互 2

### 5. 鐘舵€佹満閲嶆瀯 鈥?浠庢贩涔卞埌娓呮櫚

```
淇敼鍓嶏細while(1) { 鍙戣剦鍐?鈫?姝荤瓑 鈫?鍒ゆ爣蹇?鈫?绠楄窛绂?鈫?寤舵椂 }  // 閫昏緫绾犵紶

淇敼鍚庯細
TRIG_SEND 鈫?WAIT_ECHO 鈫?PROCESS 鈫?INTERVAL
    鈫?                                 鈹?    鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 2s 鍚?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?```

姣忎釜鐘舵€佸彧鍋氫竴浠朵簨锛岄€氳繃 `switch-case` 渚濇鍒囨崲銆?
### 6. 涓柇浼樺厛绾у垎閰?
| 瀹氭椂鍣?| 鎶㈠崰浼樺厛绾?| 浣滅敤 |
|--------|-----------|------|
| TIM2 | 0锛堟渶楂橈級 | ms 绯荤粺鏃堕挓锛岀粷涓嶅彲涓?|
| TIM1 CC | 1 | 杈撳叆鎹曡幏 |
| TIM3 | 3锛堟渶浣庯級 | 渭s 婧㈠嚭璁℃暟 |

### 7. 寰璁℃暟鍣?do-while 闃茬珵鎬?
```c
do {
    ovf = timer3_overflow_count;  // 璇婚珮16浣?    cnt = TIM3->CNT;              // 璇讳綆16浣?} while (ovf != timer3_overflow_count);
// 濡傛灉璇诲彇鏈熼棿鍒氬ソ婧㈠嚭锛宱vf 涓嶅尮閰?鈫?閲嶈
```

---

## 涓夈€佸績寰?
- 杈撳叆鎹曡幏 + 鐘舵€佹満鏄粡鍏哥粍鍚堬細**涓柇璁炬爣蹇楋紝涓诲惊鐜蛋鐘舵€?*
- 鍥炵粫璁＄畻鏄?16 浣嶈鏁板櫒鏈€闅愯斀鐨勫潙鈥斺€擟 璇█鐨勬暣鍨嬫彁鍗囪鍒欏湪宓屽叆寮忛噷鏄湡浼氬嚭 bug
- 鐘舵€佹満閲嶆瀯鑺变簡 4 灏忔椂锛屼絾杩欐槸"鑳界敤鈫掑彲缁存姢"鐨勮川鍙樷€斺€斿厛鐢绘祦绋嬪浘鍐嶅啓浠ｇ爜鏄搧寰?- 寰绾ц鏃跺櫒锛圱IM3+婧㈠嚭璁℃暟锛夋槸鍚庣画鎵€鏈夌簿瀵嗘椂搴忕殑鍩虹煶
