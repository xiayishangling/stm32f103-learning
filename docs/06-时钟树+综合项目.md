# 6.6 鏃堕挓鏍?+ 绗竴鍛ㄧ患鍚堥」鐩?
> **鑺墖**锛歋TM32F103C8T6 | **鐜**锛歏SCode + Keil + Keil Assistant  
> **涓婚**锛氭墜鍐?SystemInit銆丼ysTick 妯¤繍绠椼€佸洖璋冨嚱鏁般€佸澶栬鍗忓悓绯荤粺

---

## 涓€銆佹牳蹇冧唬鐮?
### 1. 鎵嬪啓 My_System_Init锛圚SI 鈫?HSE+PLL 72MHz锛?
```c
int My_System_Init(void)
{
    volatile uint32_t TimeOut;

    // 鈶?閫傞厤 Flash锛?2MHz 闇€瑕?2 涓瓑寰呭懆鏈?+ 棰勫彇缂撳啿
    FLASH_PrefetchBufferCmd(FLASH_PrefetchBuffer_Enable, ENABLE);
    FLASH_SetLatency(FLASH_Latency_2);

    // 鈶?寮€鍚?HSE锛堝閮?8MHz 鏅舵尟锛夛紝绛?HSERDY
    RCC_HSEConfig(RCC_HSE_ON);
    TimeOut = 0x0500;
    while ((RCC_GetFlagStatus(RCC_FLAG_HSERDY) == RESET) && (--TimeOut));
    if (TimeOut == 0) { /* 閿欒澶勭悊锛氬叧 HSE+PLL锛岃繑鍥?*/ }

    // 鈶?閰嶇疆 PLL锛欻SE 脳 9 = 72MHz
    RCC_PLLConfig(RCC_PLLSource_HSE_Div1, RCC_PLLMul_9);
    RCC_PLLCmd(ENABLE);
    TimeOut = 0x0500;
    while ((RCC_GetFlagStatus(RCC_FLAG_PLLRDY) == RESET) && (--TimeOut));
    if (TimeOut == 0) { /* 閿欒澶勭悊 */ }

    // 鈶?閰嶇疆鎬荤嚎鍒嗛锛欻CLK=72M, PCLK1=36M, PCLK2=72M
    RCC_HCLKConfig(RCC_SYSCLK_Div1);
    RCC_PCLK1Config(RCC_HCLK_Div2);
    RCC_PCLK2Config(RCC_HCLK_Div1);

    // 鈶?鍒囨崲绯荤粺鏃堕挓婧愪负 PLL
    RCC_SYSCLKConfig(RCC_SYSCLKSource_PLLCLK);
    TimeOut = 0x0500;
    while ((RCC_GetSYSCLKSource() != 0x08) && (--TimeOut));
    if (TimeOut == 0) { /* 姝婚攣锛岀瓑鐪嬮棬鐙楀浣?*/ }

    return INIT_SYSTEM_SUCCESS;
}
```

> 鍦ㄥ惎鍔ㄦ枃浠朵腑娉ㄩ噴鎺夐粯璁ょ殑 `SystemInit`锛屾敼涓鸿皟鐢ㄦ鍑芥暟

### 2. OLED 鍥炶皟鍑芥暟锛堝簱涓庡簳灞傝В鑰︼級

```c
// 閫傞厤鍑芥暟锛氬簱瀹氭帴鍙ｏ紝浣犲啓缈昏瘧锛屾敞鍐屽悗搴撴寜闇€鍥炶皟
int I2C_OLED_Display(uint8_t addr, const uint8_t *pdata, uint16_t size)
{
    int ret = I2C_Send_Bytes(g_current_i2c, addr, pdata, size, 10);
    return (ret == I2C_SEND_SUCCESS) ? 0 : 1;  // 杩斿洖鍊兼槧灏?}

void init_OLED(void)
{
    OLED_InitTypeDef cfg = {0};
    cfg.i2c_write_cb = I2C_OLED_Display;  // 娉ㄥ唽鍥炶皟
    OLED_Init(&oled, &cfg);
}
```

### 3. 缁煎悎椤圭洰涓绘祦绋?
```c
void PROCESS(void)
{
    My_System_Init();                        // 鈶?绯荤粺鏃堕挓 72MHz
    NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);

    init_USART1();                           // 鈶?涓插彛锛堜腑鏂帴鏀讹級
    Process_I2C_OLED_Display();              // 鈶?I2C + OLED
    init_SPI();                              // 鈶?SPI + Flash
    init_PC13_GPIO();                        // 鈶?LED
    CE_Init_EXTI_For_Button();               // 鈶?鎸夐敭涓柇锛堜繚瀛?鎭㈠锛?    CE_Init_NVIC_For_USART();                // 鈶?涓插彛涓柇

    Maintain_Last_State();                   // 鈶?浠?Flash 鎭㈠涓婃鐘舵€?
    while (1)
    {
        // OLED 鏇存柊锛堟爣蹇椾綅椹卞姩锛?        if (oled_update) {
            OLED_Show_LED_Status(oled_display_state);
            oled_update = 0;
        }

        // 宸﹂敭锛氫繚瀛樺綋鍓?LED 鐘舵€佸埌 Flash
        if (flag_save) { /* 娑堟姈 鈫?SPI_W25Q64_Save_Byte() */ }

        // 鍙抽敭锛氫粠 Flash 鎭㈠ LED 鐘舵€?        if (flag_load) { /* 娑堟姈 鈫?SPI_W25Q64_Read_Byte() 鈫?鎭㈠ */ }

        // LED 鐘舵€佹満锛氬父浜?/ 甯哥伃 / 闂儊
        if (led_mode == LED_MODE_ON)       LED_ON();
        else if (led_mode == LED_MODE_OFF) LED_OFF();
        else { /* 1000ms 鍛ㄦ湡闂儊 */ }
    }
}
```

### 4. 涓柇鏈嶅姟鍑芥暟锛堟爣蹇椾綅閫氫俊锛屼笉鐩存帴鎿嶄綔纭欢锛?
```c
void USART1_IRQHandler(void)
{
    if (USART_GetFlagStatus(USART1, USART_FLAG_RXNE) == SET) {
        uint16_t ch = USART_ReceiveData(USART1);
        if (ch == '1')      { led_mode = LED_MODE_ON;   oled_update = 1; }
        else if (ch == '0') { led_mode = LED_MODE_OFF;  oled_update = 1; }
        else if (ch == '2') { led_mode = LED_MODE_BLINK; }
    }
}

void EXTI1_IRQHandler(void) { flag_save = 1; EXTI_ClearFlag(EXTI_Line1); }
void EXTI2_IRQHandler(void) { flag_load = 1; EXTI_ClearFlag(EXTI_Line2); }
```

---

## 浜屻€佹牳蹇冪煡璇嗙偣

### 1. 鏃堕挓鏍戝垏鎹㈡祦绋?
```
涓婄數榛樿 HSI(8MHz)
    鈫?璁?Flash Latency=2 + 棰勫彇缂撳啿
寮€鍚?HSE(8MHz)锛岀瓑 HSERDY
    鈫?PLL = HSE 脳 9
寮€鍚?PLL锛岀瓑 PLLRDY
    鈫?璁?AHB/APB1/APB2 鍒嗛
SYSCLK 鍒囧埌 PLL 鈫?72MHz
```

### 2. Flash 绛夊緟鍛ㄦ湡

| SYSCLK | Latency | 鍘熷洜 |
|--------|---------|------|
| 0~24MHz | 0 | Flash 璺熷緱涓?|
| 24~48MHz | 1 | 闇€鎻掑叆 1 涓瓑寰呭懆鏈?|
| **48~72MHz** | **2** | 闇€鎻掑叆 2 涓瓑寰呭懆鏈?|

> 蹇樿璁?Latency = 鍒濆"瓒呴姝绘満"澶村彿鍘熷洜

### 3. 杞欢寰幆瓒呮椂 + volatile

```c
volatile uint32_t TimeOut = 0x0500;
while ((鏉′欢 == RESET) && (--TimeOut));
```

- `volatile` 闃叉缂栬瘧鍣ㄤ紭鍖栨帀寰幆鍙橀噺
- `0x0500` 鈮?1280 娆″惊鐜?鈮?1~1.6ms锛岃鐩栨櫠鎸捣鎸椂闂?- 姝ゆ椂鏃犲畾鏃跺櫒鍙敤锛屽彧鑳界敤绌哄惊鐜及绠?
### 4. SysTick 妯¤繍绠?鈥?婧㈠嚭涓轰粈涔堜笉鐢ㄦ€?
```c
uint32_t now  = GetTick();     // e.g. 0xFFFFFFF0
uint32_t prev = 0xFFFFFFF0;
// ... 璁℃暟鍣ㄦ孩鍑哄綊闆讹紝now = 0x00000010
uint32_t diff = now - prev;    // 0x00000010 - 0xFFFFFFF0 = 0x00000020 鉁?```

`uint32_t` 鍑忔硶澶╃劧妯?2鲁虏锛屽嵆浣挎孩鍑哄樊鍊间緷鐒舵纭€斺€旀墍鏈?`millis()` 绫诲嚱鏁扮殑鏁板鍩虹銆?
### 5. 鍥炶皟鍑芥暟妯″紡

```
搴撳畾涔夊嚱鏁版寚閽? 鈫? 鐢ㄦ埛鍐欓€傞厤鍑芥暟  鈫? 娉ㄥ唽鎸囬拡  鈫? 搴撴寜闇€鍥炶皟
```

鏍稿績浠峰€硷細搴撲笉缁戝畾纭欢锛屾崲 MCU 鍙渶閲嶅啓閫傞厤鍑芥暟銆?
### 6. 缁煎悎椤圭洰鏋舵瀯鍘熷垯

| 鍘熷垯 | 瀹炵幇 |
|------|------|
| 闈為樆濉炰富寰幆 | 鐘舵€佹満 + `GetTick()` 宸€兼瘮杈?|
| ISR 鍙鏍囧織浣?| 涓嶇洿鎺ユ搷浣滅‖浠讹紝涓诲惊鐜粺涓€澶勭悊 |
| 涓柇浼樺厛绾?| 鎸夐敭(鎶㈠崰0) > 涓插彛(鎶㈠崰1) |
| 鐘舵€佹寔涔呭寲 | Flash 淇濆瓨鈫掍笂鐢垫仮澶?|
| 澶氭簮鎺у埗瑙ｈ€?| `led_mode` 鐘舵€佸彉閲忕粺涓€浠茶 |

---

## 涓夈€佸績寰?
- 鎵嬪啓 `My_System_Init` 鏄鏃堕挓鏍戠悊瑙ｇ殑缁堟瀬妫€楠屸€斺€旀瘮璋冨簱娣卞埢 10 鍊?- "搴撳畾鎺ュ彛锛屼綘鍐欑炕璇?鐨勫洖璋冩ā寮忔槸闃呰/绉绘涓€鍒囩涓夋柟搴撶殑閫氱敤鎶€鑳?- 绗竴鍛ㄤ粠鐐逛寒 LED 鍒?5 涓璁惧崗鍚屸€斺€斿婧愮敤鐘舵€佸彉閲忚В鑰︺€両SR 鐢ㄦ爣蹇椾綅閫氫俊锛屾槸瑁告満鏋舵瀯鐨勫熀鐭
